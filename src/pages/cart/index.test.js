import React from "react";
import { render } from "@testing-library/react";
import Cart from "./index";
/**
test("should render component Cart", () => {
  const { getByText } = render(<Cart />);
});
*/

test("renders welcome message", () => {
  return;
});
